import { ISlotDataPresenter } from '@clientframework/slots/engine/src/ts/dataPresenter/interfaces';

export function doPrewinAnimation(
    scene: Phaser.Scene,
    eventEmitter: Phaser.Events.EventEmitter,
    data: ISlotDataPresenter
): void {
    scene;
    eventEmitter;
    data;
}
