export enum ePathSlotCommands {
    play = 'play',
    bet = 'bet',
    init = 'init',
    action = 'action',
    playbonus = 'playbonus',
}

export type BetDirectionType = '+' | '-' | 'max' | 'min' | number;
